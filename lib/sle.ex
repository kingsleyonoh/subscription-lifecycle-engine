defmodule SLE do
  @moduledoc """
  Subscription Lifecycle Engine.

  Manages the full subscription lifecycle — trial, active, past due,
  paused, canceled — driven by Stripe webhook events.
  """
end
